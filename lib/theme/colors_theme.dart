import 'package:flutter/material.dart';

abstract class ColorPaletteTheme {
  static const mainColor = Color.fromRGBO(3, 37, 65, 1);
  static const secondaryColor = Color.fromRGBO(1, 180, 228, 1);

  static const backgroundColor = Colors.white;
  static const foregroundColor = Colors.white;

  static const errorColor = Color.fromRGBO(172, 84, 84, 1);
}
